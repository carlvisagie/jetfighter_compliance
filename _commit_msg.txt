feat: autonomous acquisition engine -- activate, route, and convert leads

Full platform pass to close the gap between acquisition discovery and
paying customer intake, with minimal human involvement.

Acquisition engine activation:
- render.yaml: KYC_SAFE_MODE=false, schedulers enabled, manual acquisition
  and knowledge/observability modules all activated for production
- Acquisition now runs autonomously on schedule (APScheduler enabled)

Routing fix (inquiry to shop with ref attribution):
- routing.py: default destination changed from /ui/inquiry.html to /ui/shop.html
- shop.html: all upload CTAs now link to /ui/founding-beta (direct upload)
- shop.html: JS ref-propagation script preserves ref=LD-xxx param across
  all CTA clicks (acquisition lead attribution follows the prospect)
- founding-beta.js: reads ref from URL and appends it to the upload FormData

Intake attribution pipeline (ref stores lead_id on intake):
- server.py /api/intake/upload and /api/founding-beta/upload: accept ref form field
- intake.py process_upload: stores lead_id on intake record when ref=LD-xxx
- intake.py _link_intake_to_lead: on upload commit, updates lead status to
  intake_completed and emits acquisition_conversion alert

Approve-and-invite autonomous flow (the one human gate):
- orchestration.py approve_and_invite_lead: operator approves a lead,
  system generates tracked shop URL with ref, drafts outreach email copy
- server.py: new POST /api/operator/acquisition-intelligence/approve-lead endpoint
- control.html: Approve and Invite button on each acquisition target card;
  on success shows invite link and copyable email draft inline

Operator notification on upload (was completely missing):
- intake.py: calls alert_first_paperwork_submission on every committed upload
- alerts/engine.py: alert_first_paperwork_submission accepts lead_id kwarg,
  propagates it in alert context for acquisition attribution
- alerts/engine.py: new alert_acquisition_conversion function
- alerts/__init__.py: exports alert_acquisition_conversion
- services/email_utils.py: shared invitation email template

Tests (11 new, 1 updated):
- test_autonomous_acquisition_flow.py: routing, approve-and-invite,
  ref attribution, invalid-ref guard, email template, conversion alert
- test_acquisition_organism.py: updated routing assertion (inquiry to shop)
